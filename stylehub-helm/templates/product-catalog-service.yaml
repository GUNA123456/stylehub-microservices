apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-product-catalog-service
  labels:
    app: {{ .Release.Name }}-product-catalog-service
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-product-catalog-service
  template:
    metadata:
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: "{{ .Values.microservices.productCatalog.port }}"
      labels:
        app: {{ .Release.Name }}-product-catalog-service
    spec:
      containers:
        - name: product-catalog-service
          image: "{{ .Values.image.repositoryPrefix }}/product-catalog-service:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: {{ .Values.microservices.productCatalog.port }}
            initialDelaySeconds: {{ .Values.probes.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.periodSeconds }}
          readinessProbe:
            httpGet:
              path: /healthz
              port: {{ .Values.microservices.productCatalog.port }}
            initialDelaySeconds: {{ .Values.probes.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.periodSeconds }}
          {{- if .Values.otel.endpoint }}
          env:
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: {{ .Values.otel.endpoint | quote }}
          {{- end }}
          ports:
            - containerPort: {{ .Values.microservices.productCatalog.port }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-product-catalog-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Release.Name }}-product-catalog-service
  ports:
    - port: {{ .Values.microservices.productCatalog.port }}
      targetPort: {{ .Values.microservices.productCatalog.port }}
