apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-payment-service
  labels:
    app: {{ .Release.Name }}-payment-service
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-payment-service
  template:
    metadata:
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: "{{ .Values.microservices.payment.port }}"
      labels:
        app: {{ .Release.Name }}-payment-service
    spec:
      containers:
        - name: payment-service
          image: "{{ .Values.image.repositoryPrefix }}/payment-service:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: {{ .Values.microservices.payment.port }}
            initialDelaySeconds: {{ .Values.probes.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.timeoutSeconds }}
          readinessProbe:
            httpGet:
              path: /healthz
              port: {{ .Values.microservices.payment.port }}
            initialDelaySeconds: {{ .Values.probes.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.timeoutSeconds }}
          {{- if .Values.otel.endpoint }}
          env:
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: {{ .Values.otel.endpoint | quote }}
          {{- end }}
          ports:
            - containerPort: {{ .Values.microservices.payment.port }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-payment-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Release.Name }}-payment-service
  ports:
    - port: {{ .Values.microservices.payment.port }}
      targetPort: {{ .Values.microservices.payment.port }}
